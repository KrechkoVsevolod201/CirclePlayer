# Copy to keystore.properties and fill in your private release signing values.
# Keep keystore.properties and the keystore file out of version control.
storeFile=/absolute/path/to/circleplayer-upload-key.jks
storePassword=REPLACE_WITH_STORE_PASSWORD
keyAlias=circleplayer
keyPassword=REPLACE_WITH_KEY_PASSWORD
